---
applications:
  - name: weather
    buildpacks:
    - java_buildpack_offline
    path: ./target/weather-0.0.1-SNAPSHOT.jar
    routes:
      - route: weather-service.apps.internal
    services:
      - registry
    env:
      JBP_CONFIG_OPEN_JDK_JRE: '{ jre: { version: 11.+ }}'